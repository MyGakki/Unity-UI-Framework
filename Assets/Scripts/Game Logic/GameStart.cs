using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameStart : BaseUIForm
{
    // Start is called before the first frame update
    void Start()
    {
        OpenUIform(ConstDefine.LOGIN_FROMS);
    }

}
